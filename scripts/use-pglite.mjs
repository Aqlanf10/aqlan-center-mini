/**
 * تمهيد الرحلات التي تعمل على PGlite داخل العملية — قاعدةٌ جديدةٌ فارغةٌ كل تشغيل.
 *
 * ثلاثة أسطرٍ كانت منسوخةً في كل رحلة، وكل رحلةٍ نسيتها عملت على `DATABASE_URL`
 * الموروث من `.env.local` أو من البيئة — أي على قاعدةٍ مشتركة: تتنازع الرحلات على
 * الصفوف نفسها، وتحتاج «تنظيفًا» بحذفٍ يصطدم بحرّاس السجل المالي، وفي أسوأ
 * الأحوال تُصوَّب — بسطر تصدير واحد في طرفية أحدهم — نحو قاعدة الإنتاج.
 *
 * استيراد هذا الملف **قبل** أي استيراد لـ`lib/db` يجعل ذلك مستحيلًا بنيويًّا:
 * القاعدة في الذاكرة، تولد مع العملية وتموت معها. ولذلك يطغى هنا على البيئة
 * الصريحة عمدًا — عكس `load-env.mjs` — فالرحلة لا تُوجَّه إلى قاعدةٍ حقيقية أصلًا.
 */
process.env.SESSION_SECRET = process.env.SESSION_SECRET ?? "f".repeat(48);
process.env.USE_LOCAL_DB = "true";
process.env.DATABASE_URL = "";
