import type { Config } from "tailwindcss";

/**
 * رموز التصميم — مصدر واحد لكل لون ومسافة وظلّ.
 *
 * كان الملف يحمل خمسة ألوان، فكانت كل شاشة تخترع تدرّجاتها من ألوان Tailwind
 * الجاهزة: رماديٌّ هنا وآخر هناك، وأحمرٌ لزرّ يومي وأحمرٌ لزرّ يمحو. والنتيجة أن
 * اللون يفقد معناه — ومن لا يفرّق لونه بين «اطبع» و«احذف» يعلّم مستخدمه ألّا ينظر.
 *
 * فالألوان هنا **بأدوارها لا بأسمائها**: `danger` للمُتلف وحده، و`accent` للفعل
 * الرئيسي، و`success` لما اكتمل. وتغيير هوية المركز غدًا يصير في هذا الملف لا في
 * ستّين ملفًّا.
 */
export default {
  content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}"],
  theme: {
    extend: {
      fontFamily: {
        // خطٌّ عربي واحد للواجهة كلها، مُستضاف معنا لا من خادم بعيد.
        sans: ["var(--font-arabic)", "system-ui", "sans-serif"],
      },
      colors: {
        // كحليّ الهوية: من لوحة المركز نفسها، ممدودًا إلى تدرّج كامل.
        navy: {
          50: "#f1f5f9", 100: "#e2eaf3", 200: "#c5d5e7", 300: "#9ab6d3",
          400: "#6790b8", 500: "#4470a0", 600: "#325888", 700: "#24507c",
          800: "#1a3a5c", 900: "#0d2137", 950: "#081625",
        },
        // البرتقالي: لون الفعل الرئيسي في المركز — يُستعمل بحساب لا كخلفية عامة.
        accent: {
          50: "#fef6ec", 100: "#fde9cf", 200: "#fad09b", 300: "#f8b566",
          400: "#f6a144", 500: "#f5922e", 600: "#dd7716", 700: "#b85c12",
          800: "#8f4712", 900: "#733a12",
        },
        brand: { orange: "#f5922e", blue: "#3d7ab5" },

        // ألوان بأدوارها: تُقرأ في الكود فيُعرف قصد الشاشة منها.
        success: { 50: "#ecfdf5", 100: "#d1fae5", 300: "#6ee7b7", 500: "#10b981", 700: "#047857", 900: "#064e3b" },
        warning: { 50: "#fffbeb", 100: "#fef3c7", 300: "#fcd34d", 500: "#f59e0b", 700: "#b45309", 900: "#78350f" },
        danger:  { 50: "#fef2f2", 100: "#fee2e2", 300: "#fca5a5", 500: "#ef4444", 700: "#b91c1c", 900: "#7f1d1d" },
        info:    { 50: "#eff6ff", 100: "#dbeafe", 300: "#93c5fd", 500: "#3b82f6", 700: "#1d4ed8", 900: "#1e3a8a" },

        // خلفية التطبيق: مصدر واحد بدل لونٍ مكتوب في globals.css وآخر في الشاشات.
        canvas: "#f2f6fb",
      },
      borderRadius: { xl: "0.75rem", "2xl": "1rem", "3xl": "1.25rem" },
      boxShadow: {
        // ظلٌّ واحد للبطاقات وآخر لما يعلو فوقها. وثالثٌ لا داعي له.
        card: "0 1px 2px rgba(13, 33, 55, 0.04), 0 1px 3px rgba(13, 33, 55, 0.06)",
        raised: "0 4px 12px rgba(13, 33, 55, 0.08), 0 2px 4px rgba(13, 33, 55, 0.04)",
      },
    },
  },
  plugins: [],
} satisfies Config;
