import { NextResponse } from "next/server";

export const dynamic = "force-dynamic";

/**
 * نبض الخدمة — لفحص المنصة لا للتشخيص.
 *
 * يردّ بلا لمس قاعدة البيانات عمدًا، وهذا فرق جوهري عن `/api/health`:
 *
 * فحص المنصة يُستدعى كل بضع ثوانٍ إلى الأبد. لو مسّ القاعدة لأثقلها بلا فائدة،
 * والأسوأ أنه يربط **حياة التطبيق بحياة القاعدة**: انقطاعٌ عابر في القاعدة يجعل
 * المنصة تحكم على الخدمة بالموت فتعيد تشغيلها أو تُلغي نشرة سليمة — فيتحوّل عطبٌ
 * يزول وحده إلى انقطاع كامل في وقت الذروة.
 *
 * التشخيص مكانه `/api/health`: يُفتح عند الحاجة ويقول أيّ إعداد ناقص.
 */
export function GET() {
  return NextResponse.json({ ok: true }, { headers: { "Cache-Control": "no-store" } });
}
