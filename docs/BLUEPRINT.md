# AQLAN DENTAL OS — MASTER BLUEPRINT

**المواصفة الرسمية والدستور المعماري**
نظام إدارة وتشغيل مركز الدكتور عقلان لطب وجراحة وتقويم الأسنان

| | |
|---|---|
| **الإصدار** | v1.0.0-PROD-CONSTITUTION |
| **الجهة** | مركز د. عقلان الكامل لتقويم وزراعة وتجميل الأسنان |
| **التصنيف** | Executive Master Plan & System PRD |

> **كيف تُقرأ هذه الوثيقة:** الأقسام ١–٧ هي المواصفة الرسمية كما اعتمدها المالك.
> والقسم ٨ **تدقيقٌ صادق لحالة التنفيذ** — لأن مواصفةً لا يُقاس عليها الواقع تصير
> ورقةً يُستشهد بها ولا تُتبع. والقسم ٩ يذكر مواضع اختلاف المنفَّذ عن النصّ وأسبابها.
>
> **النصّ الرسمي الكامل** للدستور (بالتفصيل الكامل للوحدات والكيانات والرحلة
> الذهبية وبوابات القبول) محفوظ في [`CONSTITUTION.md`](./CONSTITUTION.md)،
> ومصفوفة ربط كل بندٍ بموضعه في الكود في [`COVERAGE.md`](./COVERAGE.md).

---

## ١. الغرض

دستورٌ مرجعي يضع حدًّا للعشوائية والازدواجية وتراكم الدين التقني، ويغطي دورة حياة
المريض الإدارية والسريرية والمالية والمعملية والمخزنية بلا انقطاع.

## ٢. المبادئ الخمسة الحاكمة

| # | المبدأ | المضمون |
|---|---|---|
| **1** | **One Patient — One Record** | سجل مريض واحد غير قابل للتجزئة يجمع التاريخ السريري والمواعيد والأشعة وخطط العلاج والمعامل والمالية في مصدر حقيقة واحد. |
| **2** | **One Clinical Workflow** | التشخيص يولّد خطة علاج، والخطة تولّد الخدمة المنفّذة، والخدمة تولّد الاستحقاق المالي **تلقائيًا بلا تكرار إدخال**. |
| **3** | **One Financial Source of Truth** | نموذج مالي موحّد على دفتر الأستاذ، سنداتٌ غير قابلة للتعديل (Append-Only Reversals)، وعزلٌ تامّ للعملات (YER / SAR / USD). |
| **4** | **Central Business Rules** | منطق الأعمال في خدمات النطاق على الخادم — يخدم الويب وشاشات المركز وتطبيقات الجوال بلا تكرار في الواجهة. |
| **5** | **One Auditable History** | سجل تدقيق شامل غير قابل للتعديل لكل إجراء سريري أو مالي أو إداري، بهوية المنفِّذ والتوقيت والتفاصيل. |

## ٣. المناطق التشغيلية الست

### ZONE_A — التشغيل اليومي
لوحة القيادة الميدانية · شاشة اليوم (Patient Flow Board) · جدول المواعيد · الكراسي
والانتظار · طوابير المتابعة والاستدعاء · الاتصالات وسجل المراسلات.

- لا يُسكَّن مريض على كرسي غير شاغر.
- يُمنع القفز العشوائي بين حالات المواعيد (Status Jumping).
- لكل كرسي حالة تشغيلية: متاح · مشغول · تعقيم · صيانة.
- لا تُرسل رسالة مكررة لنفس الموعد خلال أقل من ١٢ ساعة إلا بتأكيد يدوي.

### ZONE_B — المرضى والعمل السريري
سجل المرضى وكشف التكرار · ملف المريض الموحّد · مخطط FDI · خطط العلاج · الزيارات
السريرية · ملف التقويم · التحليل السيفالومتري · الأشعة والمستندات.

- رقم الملف فريد ودائم ولا يتغيّر عبر حياة المريض.
- تبويبات ملف المريض تقرأ من مصدر الحقيقة الأصلي.
- إنجاز الخدمة يحدّث حالة السن في المخطط آليًا.
- **الزيارة المغلقة لا تُعدَّل بصمت**؛ أي تعديل يُسجَّل ملحقًا (Addendum) بهوية كاتبه ووقته.
- الذكاء الاصطناعي في السيفالو **يقترح ولا يعتمد**.
- يُمنع تخزين الصور Blobs داخل PostgreSQL.

### ZONE_C — المالية والخزينة
ورديات الكاشير · سندات القبض · سندات الصرف · كشف حساب المريض ودفتر الأستاذ ·
عمولات الأطباء · دليل الخدمات والأسعار.

- لا سند قبض ولا صرف إلا بوردية مفتوحة للمستخدم.
- **يُمنع منعًا باتًّا تعديل أو حذف أي حركة مالية سابقة.**
- يُمنع حساب رصيد المريض بطرق مختلفة في شاشات متفرقة.
- لا عمولة بلا الرجوع لجدول الخدمات وقاعدة الطبيب.
- شاشات الخطط والزيارات والفوترة تستدعي دليل الخدمات حصرًا.

### ZONE_D — المعامل والموردون والمخزون
أوامر المعامل · حسابات الموردين والمشتريات · المخزون وحركات المواد.

- استلام طلبية المعمل يولّد استحقاقًا في كشف حسابه آليًا.
- نفس الاستعلام يخدم صفحة المورد والتقارير وملفات PDF.
- يُمنع تعديل رصيد المخزون كحقل رقمي دون حركة تسوية مبرَّرة.

### ZONE_E — الإدارة والرقابة
غرفة قيادة المالك (KPIs) · مركز التقارير الموحّد · سجل التدقيق · صحة النظام والنسخ الاحتياطي.

- المؤشرات من حركات مدقَّقة في دفتر الأستاذ حصرًا.
- **يُمنع برمجيًا وجود أي واجهة أو مسار يحذف أو يعدّل سجل التدقيق.**
- وجود ملف نسخة احتياطية لا يعني نجاحه بلا فحص سلامة.

### ZONE_F — الإعدادات والتهيئة
بيانات المركز والفروع · المستخدمون والصلاحيات الدقيقة · الترقيم التلقائي ·
قوالب الطباعة.

- الهوية الرسمية موحّدة على كل قوالب الطباعة والسندات.
- الواجهة ليست حاجز حماية؛ **كل نقطة نهاية تفحص الصلاحية**.
- توليد الأرقام عبر قفل تزامني ذرّي في قاعدة البيانات.
- أي مستند مالي يُطبع ثانيةً يحمل علامة «نسخة معاد طباعتها».

## ٤. الرحلة الذهبية — أربع عشرة محطة

استفسار ← تسجيل ← حجز ← تأكيد ← وصول ← انتظار وتسكين كرسي ← بدء الزيارة ← تشخيص ←
خطة علاج وموافقة ← تنفيذ وإغلاق الزيارة ← **توليد الاستحقاق تلقائيًا** ← قبض وسند ←
موعد قادم أو استدعاء ← متابعة.

**القاعدة:** لا يجوز أن تكون الخدمة السريرية موجودة والمالية لا تعرف عنها، ولا أن
تكتمل الزيارة والمتابعة لا تعرف، ولا أن يقع الدفع والصندوق لا يعرف. سلسلة واحدة.

## ٥. النطاقات الاثنا عشر ومسؤولياتها

| # | النطاق | يملك |
|---|---|---|
| 1 | Patient | الهوية، رقم الملف، كشف التكرار |
| 2 | Scheduling | المواعيد، الانتظار، الكراسي |
| 3 | Clinical | الزيارات، المخطط، الإجراءات |
| 4 | Treatment Planning | المشاكل، الخطط، المراحل |
| 5 | Ortho | حالات التقويم والميكانيكا |
| 6 | Imaging | الصور والأشعة والمستندات |
| 7 | Billing | الاستحقاقات والفواتير ودفتر المريض |
| 8 | Treasury | الورديات والسندات وحركة الصندوق |
| 9 | Accounting | دليل الحسابات والقيود والفترات |
| 10 | Payables | ذمم المعامل والموردين |
| 11 | Inventory | الأصناف والحركات والمشتريات |
| 12 | Identity & Governance | المستخدمون، الصلاحيات، التدقيق |

**القاعدة:** أي شاشة تحتاج رصيدًا تستدعي نطاقه ولا تحسبه بنفسها.

## ٦. المراحل الأربع عشرة

| المرحلة | المحتوى |
|---|---|
| 0 | الأساس المعماري والأمان والبنية — **لا ميزات قبلها** |
| 1 | النواة التشغيلية: المرضى، المواعيد، اليوم، الكراسي |
| 2 | السجل السريري الموحّد ومخطط FDI وخطط العلاج |
| 3 | المالية والخزينة الصارمة — **عالية الخطورة** |
| 4 | المعامل والموردون وسندات الصرف |
| 5 | عمولات الأطباء |
| 6 | المحاسبة والقيد المزدوج |
| 7 | التقويم التخصصي |
| 8 | السيفالومتري والأشعة |
| 9 | المخزون |
| 10 | تحليلات الإدارة |
| 11 | تجربة المريض والبوابة |
| 12 | Desktop PWA وتطبيق الطاقم |
| 13 | الخدمات الذكية — **آخر شيء** |

## ٧. المحظورات العشرة

1. لا Module جديد قبل البحث في النواة الحالية.
2. لا تكرار لمنطق حساب الأرصدة في واجهات مختلفة.
3. لا دمج للعملات في رصيد رقمي واحد.
4. لا تعديل صامت على حركة سريرية بعد إغلاق الزيارة.
5. لا تعديل ولا حذف لحركة مالية تاريخية — التصحيح بسند عكسي.
6. لا فاصلة عائمة للمبالغ — `NUMERIC(14,2)` أو أعداد صحيحة.
7. لا `MAX(id)+1` لتوليد الأرقام — قفل تزامني ذرّي.
8. لا Blobs للصور داخل PostgreSQL — تخزين كائني والبيانات الوصفية فقط في القاعدة.
9. لا اعتماد تلقائي لأي قرار من الذكاء الاصطناعي.
10. لا منطق أعمال داخل مكوّنات React.

---

## ٨. حالة التنفيذ — تدقيق صادق

### المبادئ الخمسة

| المبدأ | الحالة | الدليل |
|---|---|---|
| 1 · One Patient | 🟢 **منفَّذ** | ملف موحّد بتبويباته (نظرة عامة، مخطط، خطة، حساب، مواعيد، زيارات). وكشفُ التكرار عند الإنشاء، وربطُ الوصول بالملف حارسان اثنان قبله: المسجَّل الذي يصل بلا رقم يُعرض ملفّه ليُختار، ويُحذَّر ثانيةً قبل التوقيع. ولا دمج بالاسم صامتًا — `verify:identity`. |
| 2 · One Clinical Workflow | 🟢 **منفَّذ** | السلسلة كاملة: خطةٌ ببنودٍ مسعّرة من الدليل ← موافقةٌ تُقفل الاتفاق وتنقل البنود إلى المخطط مخطَّطةً ← زيارةٌ تُوثَّق وتُوقَّع فتولّد فاتورتها وتحدّث المخطط وتشطب بنود الخطة **في معاملة واحدة**. والأشعة والمستندات في تبويبٍ داخل الملف نفسه. |
| 3 · One Financial Truth | 🟢 **منفَّذ** | دفتر أستاذ واحد، عكس لا حذف، عزل عملات بسعر منسوخ، وردية إلزامية. |
| 4 · Central Business Rules | 🟢 **منفَّذ غالبًا** | المنطق في `lib/` مُختبَرًا. بقايا تجميع عرضي في أربع شاشات. |
| 5 · One Auditable History | 🟢 **منفَّذ** | سجل تدقيق يرفض التعديل والحذف في القاعدة نفسها (Triggers)، مغطّيًا الحركات المالية والورديات والصلاحيات والإعدادات وخروج البيانات. |

### المحظورات العشرة

| # | المحظور | الحالة |
|---|---|---|
| 1 | Module قبل البحث | 🟢 |
| 2 | تكرار حساب الأرصدة | 🟢 `patientLedger` واحدة للشاشة والطباعة والتقرير |
| 3 | دمج العملات | 🟢 كل عملة مستقلة، والمكافئ للتقارير فقط |
| 4 | تعديل سريري صامت | 🟢 الزيارة الموقَّعة مقفلة؛ التصحيح ملحقٌ مؤرَّخ باسم كاتبه لا استبدالٌ للنصّ |
| 5 | تعديل حركة مالية | 🟢 عكس لا حذف، وإقفال فترات |
| 6 | فاصلة عائمة | 🟢 كل حقول المبالغ `BIGINT` بوحدات صغرى (١٦ حقلًا)؛ والعائمة في العرض فقط |
| 7 | `MAX(id)+1` | 🟢 **أُصلح** — عدّادات قاعدة، وفحص تزامن يثبته |
| 8 | Blobs للصور | 🟢 الأشعة على القرص والوصف في القاعدة — `verify:documents` يفحص أعمدة الجدول ويشترط ألّا يكون فيها `bytea` |
| 9 | اعتماد آلي للذكاء | ⚪ لا ذكاء اصطناعي في النظام |
| 10 | منطق في React | 🟡 غالبه في `lib/`؛ بقايا تجميع في أربع شاشات |

### المراحل

| المرحلة | الحالة |
|---|---|
| 0 · الأساس | 🟢 مكتملة — الأمان والصلاحيات والنسخ والفحوص وسجل التدقيق |
| 1 · النواة التشغيلية | 🟢 مكتملة |
| 2 · السجل السريري | 🟢 **مكتملة** — مخطط FDI، والزيارة الموقَّعة، وسلسلة «خطة ← زيارة ← استحقاق»، والأشعة والمستندات |
| 3 · المالية | 🟢 مكتملة |
| 4 · المعامل والموردون | 🟢 مكتملة |
| 5 · العمولات | 🟢 مكتملة |
| 6 · المحاسبة | 🟢 مكتملة |
| 7 · التقويم التخصصي | 🟢 **منفَّذة** — الحالة والأسلاك وزيارات الشدّ والمثبّت، وقراءتها على الكرسي |
| 8 · السيفالومتري والأشعة | 🟢 **منفَّذة** — معايرة مليمترية، معالم وقياسات بجداول مباشرة، اعتماد يقفل وتصحيح بنسخة، وأشعة على قرصٍ دائم `verify:ceph` |
| 9 · المخزون | 🟢 **منفَّذة** — رصيد اشتقاقي من الحركات بلا عمود رصيد، صرف بقفل صف البند، تسوية بلا سبب مرفوضة، دفعات FEFO وتنبيهات `verify:inventory` |
| 10 · تحليلات الإدارة | 🟢 **منفَّذة** — غرفة القيادة للمدير: المال من دفاتر الأستاذ حصرًا (ميزان مراجعة وقائمة دخل)، وحركة الصندوق بالعملة، وذمم الدفاتر، وإشغال الكراسي على أيام العمل الفعلية، وتصدير CSV من الكائن نفسه — `verify:executive` يُثبت المطابقة ١٠٠٪ بالحساب المستقل من المستندات |
| 12–13 | 🔴 لم تبدأ (١٣ آخر شيء عمدًا) |
| 11 · تجربة المريض والبوابة | 🟢 **منفَّذة** — جلسة موقّعة بمجال منفصل عن الطاقم (كوكي آخر، لا توكن يعبر الاتجاهين)، دخول هاتف×ملف بحدّ محاولات يُقرأ من التدقيق ببصمة الهاتف، كشف حساب من `patientLedger()` حصرًا، مواعيد قادمة وتأكيد حضور بملكيته داخل SQL، استمارة صحية سجل يُضاف إليه — `verify:portal` |

| # | المحظور | الحالة |
|---|---|---|
| — | حذف أو تعديل سجل التدقيق | 🟢 مرفوض في القاعدة نفسها — `verify:audit` يحاول من اتصال مباشر ويشترط الفشل |
| — | علامة «نسخة معاد طباعتها» | 🟢 على السند والفاتورة وسند الصرف، وتظهر على الورقة الخارجة لا التي بعدها |
| — | زيارة سريرية بلا استحقاق | 🟢 التوقيع يولّد الفاتورة ويحدّث المخطط في معاملة واحدة — `verify:clinical` يُفشل جزءًا عمدًا ويشترط سقوطها كلها |
| — | اتفاقٌ يُعدَّل بعد توقيع المريض | 🟢 البنود تُقفل بالموافقة، والمستجدّ بخطةٍ جديدة — `verify:plans` يحاول الإضافة والحذف بعدها ويشترط الرفض |
| — | فوترةٌ مزدوجة لعملٍ داخل خطة أقساط | 🟡 لا يُمنع بالقوة — قد يكون الإجراء خارج الاتفاق — لكنه لا يمرّ صامتًا: تحذيرٌ قبل التوقيع |
| — | ملفٌّ ثانٍ لمريضٍ مسجَّل | 🟢 يُعرض ملفّه عند الوصول ويُحذَّر قبل التوقيع — `verify:identity` يُثبت أنه لا يُدمج بالاسم كذلك |
| — | ضياع الأشعة على قرصٍ مؤقّت | 🟢 الرفع يُرفض ما لم يُضبط تخزينٌ دائم، ويُقال في الشاشة قبل اختيار الملف |
| — | نسخةٌ احتياطية تُظنّ كاملة | 🟢 صفحة النسخ تقول إن الأشعة خارج ملف SQL، وتعطي أرشيفها |

**الخلاصة:** المرحلة صفر مكتملة، والمالية والتشغيل اليومي متقدّمان على المواصفة،
وقد أُغلقت أخطر حلقةٍ ناقصة في الرحلة الذهبية: **خطة ← تنفيذ ← استحقاق** صارت
سلسلةً واحدة يثبتها `verify:clinical` و`verify:plans`. وأُغلقت معها ثغرةُ الملف
الثاني: المريض المسجَّل الذي يصل بلا رقم جوال كان يُنشأ له ملفٌّ ثانٍ عند التوقيع،
فصار يُعرض ملفّه ليُختار — بقرارٍ بشري لا مطابقةٍ صامتة بالاسم — ويُحذَّر ثانيةً قبل
التوقيع إن بقيت الزيارة بلا ربط.

وبالأشعة والمستندات اكتملت المرحلة ٢، وبملفّ التقويم اكتملت **المرحلة ٧** — وهي ما
بُني هذا كله من أجله. والتالي بحسب الترتيب: المرحلة ٨ — السيفالومتري، ونموذجها
المعتمد WebCeph.

---

## ٩. مواضع اختلاف المنفَّذ عن النصّ

### ٩.١ ترتيب المراحل — نُفِّذ بترتيب مختلف عمدًا

المواصفة تضع المحاسبة في المرحلة ٦ والتقويم في ٧. والمنفَّذ أنهى ٣ و٤ و٥ و٦ **قبل**
المرحلة ٢. والسبب: بُنيت قبل اعتماد هذه الوثيقة، وإعادة ترتيبها الآن تعني هدم ما
يعمل ويُختبر. **والترتيب الباقي يُتبع كما هو**: المرحلة ٢ هي التالية.

### ٩.٢ مقاس سند القبض — A6 لا A5

المواصفة تحدّد A5. والمنفَّذ A6 (ربع ورقة) **بقرار صريح من المالك**: «ما نخسر ورق
كثير». والقرار التشغيلي لصاحب المركز مقدَّم على التفصيل الورقي في المواصفة —
ويُغيَّر بكلمة منه.

### ٩.٣ التقنية

المواصفة لا تفرض إطارًا. والمنفَّذ: Next.js + PostgreSQL عبر `pg` مباشرة، وجلسات
`scrypt` موقّعة، ومفاتيح تسلسلية بأرقام مستندات ذات معنى (`INV-00012`) — وهو ما
يخدم المحظور ٧ بعد إصلاحه، ويخدم مراجع الحسابات الذي يطلب «الفاتورة رقم ١٢».

### ٩.٤ قاعدة تسلسل البناء

> لا وحدة كبيرة جديدة قبل أن يمرّ على البرنامج **أسبوع تشغيل حقيقي** في المركز.
> وما يُبنى بعدها يُختار مما كشفه الاستعمال، لا مما تقترحه قائمة.

المواصفة نفسها تقول: «وجود وحدات كثيرة لا يعني أن النظام منظم». والنظام السابق فيه
كل هذه الوحدات ولم يُستعمل يومًا. فالسعة ليست المقياس — الاستعمال هو.
