# المرحلة ٤ب — تدقيق الإغلاق

كلُّ بندٍ هنا قيدٌ نصَّ عليه المالك حرفًا. والجواب «نعم» لا يُقبل بلا دليلٍ
يُشار إليه بملفّه.

| # | القيد كما نُصّ عليه | الحال | الدليل |
|---|---|---|---|
| ١ | **لا محرّك سعةٍ ثانٍ.** ما بُني في ٤أ يُحفظ ويُبنى عليه. | ✅ | `judgeFullCapacity` امتدادٌ في `lib/capacity.ts` بجوار `judgeCapacity`؛ و٢٥ بند ٤أ لا تزال خضراء بلا تعديل. وحارسٌ يمنع أيّ باب حجزٍ من العودة إلى `checkSlot`. |
| ٢ | **لا مفاتيح إعداداتٍ مرقَّمة** من نوع `visit_type_1_duration`. | ✅ | الخدمات جدول `appointment_services`. والمفاتيح الأربعة المضافة سياساتٌ عامة لا حقولُ خدمة: ورديةٌ ثانية، احتياطي طوارئ، حدُّ مرضى جدد. |
| ٣ | **لا حذف صلبًا** لنوع زيارةٍ أُشير إليه في مواعيد. | ✅ | لا مسار حذفٍ في الواجهات أصلًا (`route.ts` فيه `GET/POST/PATCH` فقط)، ولا زرَّ حذفٍ في الشاشة. التعطيل يمنع الجديد ويُبقي القديم. |
| ٤ | **الاسم العربيّ ليس الهوية.** | ✅ | `code` فريدٌ بنمطٍ لاتينيّ، ويتجاهله التعديل صراحةً في `lib/appointment-service-input.ts`. |
| ٥ | **لا فاصلَ عالميًّا واحدًا** مفروضًا على الجميع. | ✅ | `buffer_before_minutes` و`buffer_after_minutes` عمودان في الخدمة. لا مفتاح إعداداتٍ عالميّ لهما. |
| ٦ | **لا إعادة حسابٍ رجعيّ** لما حُجز. | ✅ | لقطةُ المدّة والفاصلين تُكتب مع الموعد، و`windowOf` في المحرّك يقرأ لقطة الموعد لا إعداد الخدمة. وبندٌ في `capacity-full.test.ts` يُثبته بموعدٍ فواصلُه تخالف خدمته. |
| ٧ | **لا تعديل على `0001` ولا على أيّ هجرةٍ مُدمجة.** | ✅ | `migrations/0007_...sql` ملفٌّ جديد. و`git diff` على المدى `29b63a5..HEAD` لا يمسّ `0001`–`0006`. |
| ٨ | **لا ادّعاء تزامنٍ من PGlite.** | ✅ | كلُّ ادّعاءات التزامن في `__tests__/postgres/` على PostgreSQL حقيقيّ. وأُثبت أن المرصد يرى العطب: السباق نفسه بلا قفل أعطى ١٠ صفوفٍ على ٣ كراسٍ. |
| ٩ | **لا إضعاف اختبارٍ ليخضرّ.** | ✅ | ثلاثة عيوبٍ كشفتها الاختبارات أُصلحت في الشيفرة لا في الاختبار: ترتيب الإنشاء في `ensureSchema`، والنافذة بعرض صفر، وتسريب مرجع المصفوفة في `searchServices`. والبند الوحيد الذي عُدِّل عُدّل ليصف الإصلاح لا ليخفي العيب. |
| ١٠ | **الوكيل الذكي ليس مديرًا** — لا مسار تجاوزٍ خاصّ به. | ✅ | يمرّ من `bookAppointment` نفسها؛ والصلاحية من `context.permissions` لا من كونه وكيلًا؛ والتجاوز يلزمه سببٌ كما يلزم الاستقبال. ١٦ بندًا في `ai-capacity-enforcement.test.ts`، أُثبت سقوطها بإعادة الثغرة عمدًا. |
| ١١ | **تخصيص الكرسيّ لا يبقى في القاعدة بلا مستهلك.** | ✅ | اختيارٌ في `QuickAppointmentModal`: «تلقائي» أو كرسي ١..ن، والعددُ من `clinic.chairs` لا رقمًا ثابتًا، ويُعطَّل الحقل لخدمةٍ لا تشغل كرسيًّا. والتحقّق من الرقم في الخادم أيضًا. |

## ما لم يُبدأ — بنصّ الطلب

**المرحلة ٥ (قائمة الانتظار).** لم يُكتب لها سطر.

## ما بقي مؤجَّلًا بمبرّر

- **السباق عبر عمليّتين منفصلتين** (نسختا Vercel مقابل Railway): القفل
  `pg_advisory_xact_lock` استشاريّ على مستوى العنقود، فالآليّة هي نفسها المُثبتة —
  لكنّ الحالة متعدّدة النسخ استُنتجت ولم تُقَس.
- **الدَّين #84**: عقد المخطط وُلّد على PostgreSQL 16 لا 18. لم تمسّه هذه المرحلة.
