# تقرير تحصين الإنتاج — P1 (قاعدة البيانات والمال والنسخ والتخزين)

> **الخط الأساس:** `main` = `bd9a3997823cc20fbb64ca9c96fe9c0bdef576bf` (دمج P0).
> **الفرع:** `hardening/production-readiness-p1` (Draft PR #13 — لم يُدمج).
> **جولة المراجعة المستقلة الأولى:** رُوجع HEAD `2ae5198` — النتيجة NOT APPROVED
> بتسعة موانع؛ هذه الجولة (P1-FIX-1 … P1-FIX-9) تغلقها كلها دون إعادة بناء P1.
> **الحكم الصريح:** النظام **ليس Production-Ready بالكامل بعد** — هذا تقرير P1
> وحده؛ البوابة النهائية بعد المراجعة الثانية وP2. كل بند أدناه يقول: ما أُثبت،
> وما لم يُثبت، بلا ادّعاء.

## الخلاصة التنفيذية

حُوِّل الأساس من «يعمل واختباراته قوية» إلى «قاعدة بيانات وترحيلات ومعاملات
واستعادة وتخزين **قابلة للإثبات** على PostgreSQL حقيقي». أُضيف نظام هجرات مُرقَّم
مع **مجسّ توافق خط أساس قوي** (P1-FIX-1) و**قفل advisory للمهاجرين المتزامنين**
(P1-FIX-2) واعتماد لا يمسّ بيانات الإنتاج، وأُثبت التزامن المالي (إقرارات AI،
الدفعات، **الردود الجزئية المتزامنة**، WAC) على قاعدة حقيقية باتصالين متزامنين
فعلًا، وحُصّن السجل المالي بtriggers على مستوى القاعدة **تمنع UPDATE وDELETE
معًا** (P1-FIX-3)، و**رُبط مفتاح الإعادة ببصمة الطلب الكانونية** (P1-FIX-4)
وعُرِّفت semantics الردود الجزئية بمجموع لا يتجاوز الأصل تحت قفل صفّي
(P1-FIX-5)، وصار **إهلاك المواد بحسب نسبة وقت الحدث** (P1-FIX-6)، وكُشف دوام
التخزين بجذر موثَّق لا بمجرد مسار مطلق (P1-FIX-7)، و**صُنِّفت بيئة هدف أدوات
القاعدة بنيويًّا** فرفض الإنتاج صار غير قابل للتجاوز بعلم (P1-FIX-8)، وأُقفلت
سياسة TLS للإنتاج بصدق (P1-FIX-9). مسار الاستعادة أعيد بناؤه على نمط
«تحقّق كامل قبل أي لمس + staging/cutover» وأُثبت بتدريب آلي كامل.

**أرقام:** 115 ملف اختبار وحدة (1257 اختبارًا) + 7 ملفات تكامل PostgreSQL حقيقي
(53 اختبارًا) كلها خضراء. CI يشغّل typecheck + unit + postgres + audit + build.

## ما أُثبت (مُختبرًا على PostgreSQL حقيقي)

| المجال | الإثبات |
|---|---|
| **P1.4 إقرارات AI** | طلبان متزامنان (اتصالان مختلفان) على نفس confirmation ⇒ claim واحد فقط ينجح؛ عشر مطالبات ⇒ فائز واحد؛ التراجع يحرر الإقرار؛ TTL التنظيف يعمل؛ القيد الفريد 23505 مباشرةً |
| **P1.5 الدفعات + P1-FIX-4/5** | نفس مفتاح الإعادة × 10 متزامنة ⇒ سند واحد (9 replay)؛ **نفس المفتاح بعملية مختلفة (متزامنة أو متتابعة) ⇒ idempotency_conflict 409 لا replay لعملية أخرى**؛ دفعتان متزامنتان ⇒ لا Lost Update؛ **10000 ⇒ ردّ 3000 ثم 7000 مسموحان وأي زيادة مرفوضة؛ ردّان متزامنان يتجاوزان المتبقي ⇒ ينجح الآمن فقط (FOR UPDATE على صف الأصل)**؛ ردّ بعملة مختلفة ⇒ DENY؛ عملات متزامنة بصماتها |
| **P1-FIX-1 خط الأساس** | قاعدة P0 قائمة سليمة ⇒ اعتماد بلا فقد بيانات؛ **7 حالات انحراف (عمود ناقص/نوع مختلف/قيد فريد ناقص/مفتاح أجنبي ناقص/فهرس حرج ناقص/trigger ناقص/إبطال مختلف) ⇒ BASELINE_SCHEMA_MISMATCH ولا يُسجَّل 0001**؛ db:status يعرض الفروق الحقيقية نصًّا |
| **P1-FIX-2 القفل** | مهاجران متزامنان ⇒ واحد يطبّق والثاني ينتظر ثم يرى up-to-date؛ **SQL الهجرة نُفِّذ مرة واحدة بالضبط (كناري بعدّاد)**؛ القفل يُفكّ بعد فشل الهجرة فالمحاولة التالية تعمل فورًا |
| **P1-FIX-3 append-only** | SQL DELETE لدفعة/مصروف/حركة مخزون ⇒ DENY من القاعدة نفسها (trigger)؛ حذف مريض له تاريخ مالي ⇒ رفض صريح والدفعات باقية حرفيًّا؛ المصروف يُصحَّح بقيد معاكس موثَّق (voidExpense) والأصل باقٍ |
| **P1.9 المخزون/WAC** | شراء×صرف متزامنان ⇒ متسق؛ صرفان متزامنان ⇒ لا خصم مفقود؛ رصيد لا يكفي ⇒ واحد فقط ينجز (لا Oversell ولا سالب)؛ WAC deterministic |
| **P1.14 الاستعادة** | تدريب كامل: بيانات معروفة → نسخة → هدف معزول مدمَّر → استعادة → تحقق العدّات/المال/العملات/علاقات الردّ/WAC/العدّادات/بصمات المستندات؛ أرشيف فاسد ⇒ الهدف لم يُمس إطلاقًا |
| **P1.22 الهجرات** | قاعدة فارغة ⇒ كل الهجرات؛ قائمة ⇒ اعتماد بعد مجسّ قوي؛ ثانية ⇒ no-op؛ فشل وسط الطريق ⇒ تراجع كامل وإعادة محاولة تنجح؛ بصمة مخالفة ⇒ اكتشاف |
| **P1-FIX-6 النسب وقت الحدث** | حدث A قبل تغيّر النسبة (20%) وحدث B بعده (30%) ⇒ التقرير يحسب كلًّا بنسبته؛ **تغيير النسبة لاحقًا (50%) لا يغيّر النتيجتين**؛ ما قبل أول سجل = unrated لا صفر صامت |

## أعطاب كامنة كشفها التدريب وأُصلحت (قبل أن تصيب الإنتاج)

1. **مستندات النسخة الكاملة لم تكن قابلة للاستعادة أصلًا:** الكاتب يخزّن المدخل
   باسم مؤمَّن (`ab_cd_hash.png`) والقارئ القديم يبحث بالمفتاح الأصلي
   (`ab/cd/hash.png`) — كل استعادة كاملة كانت ستفشل في مرحلة المستندات.
2. **أعمدة المصفوفات (TEXT[]) تُفشل الاستعادة:** صار التسلسل واعيًا بنوع العمود.
3. **reset العدّارات يتعثر على المفاتيح النصّية:** الآن يقتصر على id العددي
   ذات nextval فعلًا.
4. **تجميد اتصال في `recordPayment`:** مع N دفعات متزامنة تُستنفد اتصالات الpool
   ويتجمّد المسار — الآن النواة المعاملاتية تحرّر الاتصال قبل الترطيب.
5. **الخصم التلقائي للمواد بلا قفل ولا فحص رصيد (Oversell صامت):** الآن
   `FOR UPDATE` + تحقق رصيد داخل معاملة التوقيع.
6. **(P1-FIX-4) إعادة سند عملية مختلفة كreplay:** نفس Idempotency-Key بمحتوى
   مغاير كان يعيد السند الأول كأن الطلبية نجحت مرتين — الآن بصمة الطلب الكانونية
   تجعلها تعارضًا 409.
7. **(P1-FIX-6) إعادة تسعير تاريخي بصمت:** تقرير يعبر تغيير نسبة كان يطبّق النسبة
   الجديدة على أحداث أقدم — الآن كل حدث بنسبته وقت وقوعه.

## القرارات المحاسبية الموثَّقة

| البند | القرار | مكان التفصيل |
|---|---|---|
| **P1.6/P1-FIX-3 السجل المالي** | append-only بقوة triggers على مستوى القاعدة تمنع **التعديل والحذف معًا** (payments/expenses/inventory_movements)؛ التصحيح ردّ/إبطال بقيد معاكس/تسوية صريحة؛ حذف مريض له تاريخ مالي ⇒ رفض صريح؛ أي purge قانوني/GDPR مستقبلًا workflow منفصل مصرَّح ومدقَّق. استثناءان موثَّقان: `payable_id` في expenses (مسار تنظيف قائم)، والأرصدة الافتتاحية (حالة لا حدث) | migration 0005 |
| **P1-FIX-5 الردود** | **الردود الجزئية هي النموذج** (قيد الردّ الواحد أُزيل): الأصل دفعة لنفس المريض، بعملته، بسعر صرفه snapshot، ومجموع الردود ≤ الأصل داخل معاملة مع FOR UPDATE — لا نموذج مختلط | migration 0003 + recordPayment |
| **P1.7 العملات** | العملات كيانات مستقلة؛ التجميع عبر `base_amount_minor` فقط (سعر الصرف snapshot لحظة الدفع على الصف) — والردّ يرث snapshot الأصل | اختبارات money-currency |
| **P1.8 المال عدد صحيح** | كل أعمدة المال BIGINT؛ تحويل القارئ يرفض الكسري/خارج 2^53 صراحةً | lib/db.ts toMinor |
| **P1.10/P1-FIX-6 نسب المواد** | سجل فعّال **TIMESTAMPTZ append-only** (لا UNIQUE يومي ولا ON CONFLICT)؛ الحالية+التاريخ بمعاملة واحدة؛ التقرير يحلّ النسبة **وقت حدث التحصيل**؛ ما قبل أول سجل = غير مقيَّم | migration 0004 |
| **P1.11 أسعار الخدمات** | snapshot قائم ومُثبت: بنود الفاتورة/الخطة تحفظ الاسم والسعر لحظة الحدث؛ التخميني موسوم حتى اعتماد صريح | اختبارات service-pricing |
| **P1.15/P1-FIX-8 الاستعادة** | staging/cutover: لا استعادة مباشرة على الإنتاج — **restore-full يرفض بنيويًّا production وunknown-remote مهما كانت بيئة الجهاز** | DISASTER_RECOVERY.md |

## البنية التحتية

- **Pool (P1.20):** `max` قابل للضبط بـ`DB_POOL_MAX` (1–20، افتراضي 3)؛ مهلات
  صريحة؛ `application_name`. **لا إعادة محاولة تلقائية للكتابات المالية عمدًا** —
  الإعادة قرار الطبقة الأعلى بمفاتيح idempotency صريحة **مرتبطة ببصمة الطلب**.
  مساعد `withTransaction` بتراجع مضمون بلا تسريب اتصال.
- **TLS (P1.19 + P1-FIX-9):** `lib/db-tls.ts` — **المحلي/CI: sslmode=disable
  مسموح. الإنتاج البعيد: sslmode=disable ⇒ رفض فوري** (التطبيق يفشل في الإقلاع
  بصوت عالٍ — لا قناة مفتوحة إلى قاعدة إنتاج). `PGSSL_ROOT_CERT` مضبوط ⇒ تحقق
  كامل (rejectUnauthorized=true مع CA). البعيد بلا CA ⇒ تشفير بلا تحقق **مخاطرة
  متبقية معلنة** مع تحذير مسجَّل عند كل إنشاء pool — لا تُسمّى verified.
  `sslmode=verify-full` بلا CA ⇒ رفض فوري. **ملاحظة صدق: لم نتحقق أن Railway
  يتيح تنزيل شهادة CA من لوحة قاعدة PostgreSQL — لا يُفترض ذلك؛ الطريقة
  المثبتة للتحقق الكامل: شهادة جذر من مصدر تثق به تُرفع للخدمة ويُضبط
  `PGSSL_ROOT_CERT`.** لم تُغيَّر بيئة الإنتاج في P1.
- **تصنيف بيئة الهدف (P1-FIX-8):** `lib/db-target.ts` — `DATABASE_ENVIRONMENT=
  test|development|staging|production` الصريح هو المصدر الأول (قيمة غير صالحة ⇒
  خطأ fail-closed لا «غير مصنَّف»)؛ بدونه: محلي ⇒ development، بعيد داخل
  Railway ⇒ production، وإلا **unknown-remote**. `db:migrate --apply` و
  `restore:full` يُرفضان بنيويًّا لproduction وunknown-remote — **لا علم يتجاوز
  في P1**، والقراءة الآمنة (status/dry-run) مسموحة.
- **التخزين الدائم (P1.17 + P1-FIX-7):** `lib/storage-readiness.ts` — المسار
  المطلق وحده ليس دليل دوام: الإنتاج يتطلب DOCUMENTS_DIR داخل **جذر موثَّق** —
  إما `DURABLE_STORAGE_ROOT` صريح (احتواء آمن بpath.relative لا startsWith) أو
  قرص مثبت فعليًّا بنظام ملفات دائم يظهر في `/proc/mounts` (كشف Railway
  الاختياري). `/app/data/documents` بلا قرص ⇒ **غير دائم** (الحالة المركزية
  المطلوبة). `/api/health`: **ready=false في الإنتاج إن كان التخزين غير مضبوط/
  مؤقّت/غير قابل للكتابة**، والناقص يظهر في قائمة الأسباب.
- **أمان المسارات (P1.13):** `lib/safe-path.ts` — الحل بفك المكوّنات لا
  `startsWith`؛ رفض المطلق و`..` وnull bytes؛ كشف الوجهات المكررة.
- **أمان الملفات (P1.18):** كما كانت (قائمة MIME، 20MB، مفاتيح بصمية، عزل
  طبيب/مريض، nosniff، no-store) — مُثبتة بالمختبرات وبقيت التغطية القائمة.

## ما لم يُثبت بعد (صراحة كاملة)

1. **لا نسخ مجدولة تعمل** — RPO/RTO في DISASTER_RECOVERY أهداف مقترحة لا مُحقَّقة.
2. **لا migration نُفِّذ على الإنتاج** — أول تشغيل (اعتماد خط الأساس بعد المجسّ
   القوي) قرار ما بعد الاعتماد.
3. **التحقق الكامل من سلسلة شهادة PostgreSQL على Railway لم يُجرَّب** — الوضع
   المُشغَّل حاليًا «مشفَّر بلا تحقق» مخاطرة متبقية معلنة؛ والطريق موثَّق
   (PGSSL_ROOT_CERT بشهادة تثق بها). لم نتحقق أن Railway يتيح تنزيل CA — لا
   يُفترض.
4. **PGlite ما زال محاكي التطوير المحلي** — التزامن يُثبت على PostgreSQL فقط.
5. `ensureSchema` لم يتقاعد — الازدواج المُوثَّق قائم حتى P2.
6. **WAC يُشتق من كامل التاريخ عند كل قراءة** — checkpoint مُداوم قرار P2.
7. عمليات الأعمدة المحسوبة النقدية للعمولات تعتمد resolveDoctorEffectivePolicy —
   مراجعة عميقة ضمن P2.
8. **كشف أقراص Railway من `/proc/mounts` منطق عام وقابل للحقن ومُختبر بوحدة
   ثابتة** — شكله الفعلي داخل نشر Railway يُصادق عليه أول تشغيل تشغيلي
   (DURABLE_STORAGE_ROOT الجذر الصريح يعمل بغض النظر).

## الالتزامات

- ❌ لا Merge (Draft PR فقط — بانتظار المراجعة المستقلة الثانية لP1).
- ❌ لا Ready-for-Review تحويل.
- ❌ لا Deploy يدوي.
- ❌ لا تعديل Production Database (كل الإثبات على قواعد معزولة).
- ❌ لا Production Restore (التدريب على أهداف معزولة تُدمَّر داخل الاختبار).
- ❌ لا Force Push ولا تغيير تاريخ. لا P2.
