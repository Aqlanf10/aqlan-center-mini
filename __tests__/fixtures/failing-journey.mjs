/* رحلة مصطنعة تسقط دائمًا — وقودُ اختبار الانحدار وحده: تُثبت أن سقوط رحلةٍ
   واحدة يجعل خروج verify:ci واحدًا. ليست في سجلّ الرحلات ولا تُشغَّل في CI. */
console.error("رحلة مصطنعة ساقطة — للاختبار");
process.exit(1);
